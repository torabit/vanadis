# The shell the demo's prompt pane runs, so no part of the recorder's own rc files shows up
# in the frame. demo/bin/demo-env has already put the demo's environment in place.
#
# starship re-reads its config on every prompt, which is why this pane follows a theme with
# no reload command anywhere.

export PS1='$ '
eval "$(starship init bash)"
